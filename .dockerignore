# Build artifacts and dependencies are produced inside the image.
**/node_modules
**/dist

# Never bake developer-local config into a production bundle: Vite inlines
# import.meta.env.VITE_* at build time, so a stray .env ships to prod.
**/.env
**/.env.*.local

.git
.gitignore
